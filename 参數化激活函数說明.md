# 參數化激活函數說明

## 📊 概述

在 `mlp_advanced.py` 中，Deep 模型（20層）的**前12層**使用了可學習的參數化激活函數，取代標準的 ReLU。這種設計能自動學習最佳的激活函數形態，提升模型表達能力。

---

## 🎯 兩種參數化激活函數

### 1. **ParametricTanh（參數化雙曲正切）**

**公式**: `output = α * tanh(β * x) + γ`

```python
class ParametricTanh(nn.Module):
    def __init__(self, alpha=1.0, beta=1.0, gamma=0.0):
        super().__init__()
        self.alpha = nn.Parameter(torch.tensor(alpha))  # 縮放參數
        self.beta = nn.Parameter(torch.tensor(beta))    # 輸入縮放
        self.gamma = nn.Parameter(torch.tensor(gamma))  # 偏移參數
    
    def forward(self, x):
        return self.alpha * torch.tanh(self.beta * x) + self.gamma
```

### 2. **ParametricSigmoid（參數化Sigmoid）**

**公式**: `output = α * sigmoid(β * x) + γ`

```python
class ParametricSigmoid(nn.Module):
    def __init__(self, alpha=1.0, beta=1.0, gamma=0.0):
        super().__init__()
        self.alpha = nn.Parameter(torch.tensor(alpha))  # 縮放參數
        self.beta = nn.Parameter(torch.tensor(beta))    # 陡峭度
        self.gamma = nn.Parameter(torch.tensor(gamma))  # 偏移參數
    
    def forward(self, x):
        return self.alpha * torch.sigmoid(self.beta * x) + self.gamma
```

---

## 🔧 Deep模型12層參數化激活配置

根據 `mlp_advanced.py` 中的設定，Deep 模型的前12層交替使用 Tanh 和 Sigmoid：

| 層 | 激活類型 | α (縮放) | β (陡峭度) | γ (偏移) |
|---|---------|---------|-----------|---------|
| Layer 1 | Tanh | 1.5 | 1.2 | 0.1 |
| Layer 2 | Sigmoid | 1.8 | 1.5 | -0.3 |
| Layer 3 | Tanh | 1.3 | 1.1 | 0.05 |
| Layer 4 | Sigmoid | 1.6 | 1.3 | -0.25 |
| Layer 5 | Tanh | 1.2 | 1.0 | 0.08 |
| Layer 6 | Sigmoid | 1.7 | 1.4 | -0.35 |
| Layer 7 | Tanh | 1.4 | 1.15 | 0.06 |
| Layer 8 | Sigmoid | 1.5 | 1.2 | -0.28 |
| Layer 9 | Tanh | 1.25 | 1.05 | 0.04 |
| Layer 10 | Sigmoid | 1.4 | 1.1 | -0.22 |
| Layer 11 | Tanh | 1.1 | 0.95 | 0.02 |
| Layer 12 | Sigmoid | 1.3 | 1.0 | -0.18 |

**Layer 13-19**: 使用標準 ReLU  
**Layer 20**: 輸出層（BCEWithLogitsLoss）

---

## 📈 參數意義解釋

### Alpha (α) - 輸出縮放因子
- **物理意義**：控制激活函數輸出的幅度
- **α > 1**：放大輸出，增強信號傳遞
- **α < 1**：衰減輸出，防止過度激活
- **配置範圍**：1.1 ~ 1.8

### Beta (β) - 輸入縮放因子（陡峭度）
- **物理意義**：控制激活函數的陡峭程度
- **β > 1**：曲線更陡峭，激活更快
- **β < 1**：曲線更平緩，減少飽和
- **配置範圍**：0.95 ~ 1.5

### Gamma (γ) - 偏移量
- **物理意義**：控制激活函數的中心位置
- **Tanh**: 使用小正偏移（0.02 ~ 0.1）
- **Sigmoid**: 使用負偏移（-0.35 ~ -0.18）避免輸出過高
- **配置範圍**：-0.35 ~ 0.1

---

## 🔧 設計策略

### 1. 交替使用 Tanh 和 Sigmoid
```
Layer 1:  Tanh    → 輸出範圍約 [-1.4, 1.6]
Layer 2:  Sigmoid → 輸出範圍約 [-0.3, 1.5]
Layer 3:  Tanh    → 輸出範圍約 [-1.25, 1.35]
...（交替進行共12層）
Layer 13+: ReLU   → 標準激活（穩定輸出）
```

**為何交替使用？**
- **Tanh**：輸出中心對稱，保持正負信號
- **Sigmoid**：輸出正向，與BatchNorm配合良好
- **交替使用**：增加網絡表達能力

### 2. 漸進式調整
- **前幾層**：調整幅度較大（α=1.5~1.8），學習底層特徵
- **中間層**：調整幅度中等（α=1.3~1.5）
- **後面層**：調整幅度較小（α=1.1~1.3），保持穩定性

### 3. 偏移設計
- **Tanh層**：小正偏移（0.02~0.1），輕微打破對稱性
- **Sigmoid層**：負偏移（-0.35~-0.18），平衡輸出範圍

---

## 📊 訓練後參數學習結果

程式會自動記錄參數變化，儲存於 `results/advanced/parameter_comparison.csv`：

```
Layer,Activation,α_Before,α_After,α_Change(%),...
Layer 1,Tanh,1.500,1.576,+5.08%,...
Layer 2,Sigmoid,1.800,1.370,-23.91%,...
...
```

### 可視化輸出
- `results/advanced/parameter_changes.png` - 參數變化柱狀圖
- `results/advanced/activation_shapes.png` - 激活函數形狀對比
- `results/advanced/activation_distribution_comparison.png` - 激活值分布

---

## 🆚 與標準 ReLU 的對比

| 特性 | 標準 ReLU | 參數化 Tanh/Sigmoid |
|-----|---------|-------------------|
| 參數數量 | 0 | 3個/層（α, β, γ）|
| 梯度流動 | 單向（x>0時）| 雙向且可調整 |
| 輸出範圍 | [0, +∞) | 可學習範圍 |
| 飽和問題 | 負區域死亡 | 可避免飽和 |
| 計算成本 | 極低 | 稍高（可接受）|
| 適用場景 | 淺層、快速 | 深層、高表達需求 |

---

## 🎓 理論依據

### 為什麼需要參數化激活函數？

1. **梯度消失問題**
   - 標準 Tanh/Sigmoid 在深層網絡中容易飽和
   - 參數化版本可學習最佳的縮放和偏移

2. **表達能力提升**
   - 固定激活函數限制了網絡的非線性能力
   - 可學習的激活函數增加隱藏層的靈活性

3. **適應數據分布**
   - 不同層處理的特徵分布不同
   - 參數化激活能自適應調整到最佳形態

---

## 🚀 預期效果

相較於標準 ReLU，參數化激活函數預期帶來：

✅ **更好的梯度流動**（減少梯度消失）  
✅ **更高的模型表達能力**（學習非線性關係）  
✅ **自適應激活形態**（針對數據優化）  
✅ **可能提升 AUC 性能**（實驗驗證）

---

**建立時間**: 2026-01-07  
**參考程式**: `mlp_advanced.py` 第62-85行（激活函數類定義）  
**框架**: PyTorch 2.x
✅ **更穩定的訓練過程**（自適應調整激活範圍）  
✅ **可能提升的AUC性能**（0.5~2%改進空間）

---

## 📖 參考論文

- *Parametric ReLU* (He et al., 2015)
- *Swish: A Self-Gated Activation Function* (Ramachandran et al., 2017)
- *Learning Activation Functions* (Agostinelli et al., 2014)

---

**創建日期**：2026-01-06  
**作者**：GitHub Copilot  
**模型版本**：mlp_advanced.py v2.0
