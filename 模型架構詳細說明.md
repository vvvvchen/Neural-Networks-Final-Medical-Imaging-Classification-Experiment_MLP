# MLP 模型架構詳細說明

## 三種模型架構對比

### 📊 快速對比表

| 模型名稱 | 總層數 | Dropout率 | BatchNorm層數 | 參數化激活層 | 約略參數量 |
|---------|--------|-----------|--------------|--------------|-----------|
| **Shallow** | 5層 | 0.2 | ❌ | 前3層 | ~30K |
| **Medium** | 10層 | 0.3 | 前5層 | 前6層 | ~120K |
| **Deep** | 20層 | 0.5 | 前12層 | 前12層 | ~200K |

---

## 1️⃣ Shallow MLP（淺層模型）

### 架構圖
```
輸入層(160) 
    ↓
[fc1] 160 → 128 + [Dropout 0.2] + [ParametricTanh / ReLU]
    ↓
[fc2] 128 → 96 + [Dropout 0.2] + [ParametricSigmoid / ReLU]
    ↓
[fc3] 96 → 64 + [Dropout 0.2] + [ParametricTanh / ReLU]
    ↓
[fc4] 64 → 32 + [ReLU]
    ↓
[fc5] 32 → 1 (輸出 logits)
```

### 詳細參數
- **總層數**: 5層（5個全連接層）
- **Dropout**: 0.2（20%的神經元隨機失活）
- **Hidden Units**: 128 → 96 → 64 → 32
- **Batch Normalization**: 無
- **參數化激活函數**: 前3層（如啟用）
  - Layer 1: ParametricTanh(α=1.5, β=1.2, γ=0.1)
  - Layer 2: ParametricSigmoid(α=1.8, β=1.5, γ=-0.3)
  - Layer 3: ParametricTanh(α=1.3, β=1.1, γ=0.05)
- **輸出層**: BCEWithLogitsLoss（無Sigmoid）

### 優點
- 訓練速度最快
- 適合初步實驗和基準比較
- 不易過擬合

### 缺點
- 表達能力有限
- 難以捕捉複雜非線性關係

---

## 2️⃣ Medium MLP（中層模型）

### 架構圖
```
輸入層(160)
    ↓
[fc1] 160 → 256 + [BN] + [Dropout 0.3] + [ParametricTanh / ReLU]
    ↓
[fc2] 256 → 224 + [BN] + [Dropout 0.3] + [ParametricSigmoid / ReLU]
    ↓
[fc3] 224 → 192 + [BN] + [Dropout 0.3] + [ParametricTanh / ReLU]
    ↓
[fc4] 192 → 160 + [BN] + [Dropout 0.3] + [ParametricSigmoid / ReLU]
    ↓
[fc5] 160 → 128 + [BN] + [Dropout 0.3] + [ParametricTanh / ReLU]
    ↓
[fc6] 128 → 96 + [Dropout 0.3] + [ParametricSigmoid / ReLU]
    ↓
[fc7] 96 → 64 + [Dropout 0.3] + [ReLU]
    ↓
[fc8] 64 → 48 + [Dropout 0.3] + [ReLU]
    ↓
[fc9] 48 → 32 + [ReLU]
    ↓
[fc10] 32 → 1 (輸出 logits)
```

### 詳細參數
- **總層數**: 10層（10個全連接層）
- **Dropout**: 0.3（30%的神經元隨機失活）
- **Hidden Units**: 256 → 224 → 192 → 160 → 128 → 96 → 64 → 48 → 32
- **Batch Normalization**: 前5層
- **參數化激活函數**: 前6層（如啟用）
  - 奇數層使用 ParametricTanh
  - 偶數層使用 ParametricSigmoid

### 優點
- 平衡複雜度與效率
- BatchNorm 穩定訓練
- 適中的表達能力

### 缺點
- 訓練時間較長
- 需要更多調參

---

## 3️⃣ Deep MLP（深層模型）⭐ 最強架構

### 架構圖
```
輸入層(160)
    ↓
[fc1] 160 → 384 + [BN] + [Dropout 0.5] + [ParametricTanh / ReLU]
    ↓
[fc2] 384 → 352 + [BN] + [Dropout 0.5] + [ParametricSigmoid / ReLU]
    ↓
[fc3] 352 → 320 + [BN] + [Dropout 0.5] + [ParametricTanh / ReLU]
    ↓
[fc4] 320 → 288 + [BN] + [Dropout 0.5] + [ParametricSigmoid / ReLU]
    ↓
[fc5] 288 → 256 + [BN] + [Dropout 0.5] + [ParametricTanh / ReLU]
    ↓
[fc6] 256 → 224 + [BN] + [Dropout 0.5] + [ParametricSigmoid / ReLU]
    ↓
[fc7] 224 → 192 + [BN] + [Dropout 0.5] + [ParametricTanh / ReLU]
    ↓
[fc8] 192 → 160 + [BN] + [Dropout 0.5] + [ParametricSigmoid / ReLU]
    ↓
[fc9] 160 → 128 + [BN] + [Dropout 0.5] + [ParametricTanh / ReLU]
    ↓
[fc10] 128 → 96 + [BN] + [Dropout 0.5] + [ParametricSigmoid / ReLU]
    ↓
[fc11] 96 → 80 + [BN] + [Dropout 0.5] + [ParametricTanh / ReLU]
    ↓
[fc12] 80 → 64 + [BN] + [Dropout 0.5] + [ParametricSigmoid / ReLU]
    ↓
[fc13] 64 → 48 + [Dropout 0.5] + [ReLU]
    ↓
[fc14] 48 → 36 + [Dropout 0.5] + [ReLU]
    ↓
[fc15] 36 → 28 + [Dropout 0.5] + [ReLU]
    ↓
[fc16] 28 → 20 + [Dropout 0.5] + [ReLU]
    ↓
[fc17] 20 → 16 + [Dropout 0.5] + [ReLU]
    ↓
[fc18] 16 → 12 + [Dropout 0.5] + [ReLU]
    ↓
[fc19] 12 → 8 + [ReLU] (Embedding層)
    ↓
[fc20] 8 → 1 (輸出 logits)
```

### 詳細參數
- **總層數**: 20層（20個全連接層）
- **Dropout**: 0.5（50%的神經元隨機失活）
- **Hidden Units**: 384→352→320→288→256→224→192→160→128→96→80→64→48→36→28→20→16→12→8
- **Batch Normalization**: 前12層
- **參數化激活函數**: 前12層（如啟用）
  - 奇數層使用 ParametricTanh
  - 偶數層使用 ParametricSigmoid
- **輸出層**: BCEWithLogitsLoss（無Sigmoid）

### 優點
- 最強的特徵表達能力
- 12層參數化激活函數提供豐富的非線性變換
- 12層BatchNorm確保深層網絡穩定訓練
- 高Dropout率防止過擬合

### 缺點
- 訓練時間最長
- 需要較多數據和計算資源

---

## 🔍 關鍵設計選擇說明

### 1. Dropout率的選擇
- **Shallow (0.2)**: 淺層模型容量小，適度正則化即可
- **Medium (0.3)**: 中等複雜度，適中正則化
- **Deep (0.5)**: 深層模型容量大，需要強正則化防止過擬合

### 2. Hidden Units的遞減設計
所有模型都採用**遞減式隱藏層單元數**：
- 特徵逐層壓縮
- 從高維（160）逐漸降維到輸出（1）
- 符合特徵提取→特徵融合→決策的層次結構

### 3. Batch Normalization的使用
- **Shallow**: 不使用（層數少，不需要）
- **Medium**: 前5層（穩定中層訓練）
- **Deep**: 前12層（深層網路必需）

### 4. 參數化激活函數配置
```python
# ParametricTanh: output = α * tanh(β * x) + γ
# ParametricSigmoid: output = α * sigmoid(β * x) + γ

# Deep模型12層參數化激活（交替使用）:
act1 = ParametricTanh(α=1.5, β=1.2, γ=0.1)
act2 = ParametricSigmoid(α=1.8, β=1.5, γ=-0.3)
act3 = ParametricTanh(α=1.3, β=1.1, γ=0.05)
# ... 共12層
```

---

## 📈 訓練配置

### 共同參數
- **優化器**: AdamW (lr=0.001, weight_decay=1e-4)
- **損失函數**: BCEWithLogitsLoss（更穩定的混合精度訓練）
- **Batch Size**: 512（訓練）/ 1024（驗證）
- **最大 Epochs**: 200
- **Early Stopping**: 
  - Warmup期: 50 epochs
  - Patience: 30 epochs
- **學習率調度**: Cosine Annealing（實驗B）
- **混合精度訓練**: FP16 + GradScaler

### DataLoader 優化（RTX A6000）
```python
DataLoader(
    batch_size=512,
    num_workers=12,
    pin_memory=True,
    persistent_workers=True,
    prefetch_factor=4,
    drop_last=True
)
```

---

## 💡 如何選擇模型

### 使用 Shallow MLP 如果：
- ✅ 特徵已經過充分工程處理
- ✅ 數據量較小（<1000樣本）
- ✅ 需要快速實驗和基準測試
- ✅ 計算資源有限

### 使用 Medium MLP 如果：
- ✅ 需要平衡性能和速度
- ✅ 數據量中等（1000-5000樣本）
- ✅ 需要較強的表達能力
- ✅ 有GPU但不想等太久

### 使用 Deep MLP 如果：
- ✅ 追求最佳性能
- ✅ 數據量充足（>5000樣本）
- ✅ 特徵高維且複雜（160維）
- ✅ 有足夠的計算資源（GPU + 時間）
- ✅ **本項目推薦選擇** ⭐

---

## 📚 程式碼位置

查看 `mlp_advanced.py` 中的類定義：
- **ShallowMLP**: 約第108-145行（5層）
- **MediumMLP**: 約第148-225行（10層）
- **DeepMLP**: 約第228-350行（20層）

---

**建立時間**: 2026-01-07  
**框架**: PyTorch 2.x + CUDA + 混合精度  
**任務**: 二元分類（良性 vs 惡性）
